# Merging PDF Files (Unsequenced)

The Python Script titled "Merge.py":
- Scans the current folder where Merge.py is in
- Looks for PDF files only
- then Merges it

However, the merged PDF is of random sequence